class Solution {
public:
/*
construct another array of length n of which the nums2 should be either all odd or all even
nums2[i] = nums1[i]
nums2[i] = nums1[i] - nums1[j]
*/
    bool uniformArray(vector<int>& nums1) {
        /*int n = nums1.size();
        vector<int> nums2(n);
        bool parity = false;
        for(int i = 0; i<n;i++){

        }*/
        return true;
    }
};